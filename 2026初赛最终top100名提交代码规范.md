## 2026初赛最终top100名提交代码规范

## 数据文件夹 /data

初赛代码不需要选手提交镜像文件。选手无需提交初始的训练数据集以及评测数据集，我们会把初赛提供的训练集放到 `/data`文件夹下。注意的是，初始的时候`data`文件夹会被清空。选手如果有对初始训练数据集进行处理，如清洗/数据增强等操作，应在同级的`processed_data`文件夹下提供处理的输出示例。选手如果有补充数据，原则上应在同级的`private_data`文件夹下提供所有的补充数据（如外部数据量过大无法全部提交，请至少提交补充数据的说明文档，说明数据总量、来源以及给出数据示例）。

数据文件夹的结构示例：

```Plaintext
project
    |--data
        |-- train
            |-- image_id_1.png
            |-- ...
        |-- eval
    |--processed_data        
        |-- processed_image_id_1.png
    |--private_data
        |-- private_image_id_1.png

```

## 代码文件夹 /code

- 请确保实验结果可以由提交的代码复现，所使用到的**所有****源码**（包含数据处理代码、训练代码、推理代码等）都要包含在提交的文件中。
- 比赛中所使用的代码等需要存放在`code/`文件夹下。
- 选手训练的最优模型参数需要存放在`checkpoint/`文件夹下。如果识别和检测是采用的不同模型，那么在`checkpoint/`文件夹下建立子文件夹存放不同的模型。
- 比赛中所使用的**策略、模型和算法**需要在`README`文件中详细描述。
- 所使用的依赖（操作系统版本，Python版本，需要安装的Python package，使用到的TensorFlow，PyTorch等的版本， CUDA 版本等）都需要在`README`文件中写明。
- 不建议在正式评测时依赖公网下载模型、数据或代码。
- 请提供数据处理阶段的代码，能够实现`processed_data`文件夹下的输出示例效果。
- 请提供train.py 或main.py 或run.sh 等文件，确保可以通过执行该文件来运行训练阶段和推理阶段所有代码（训练和推理阶段可分成两个启动脚本，也可以一个脚本直接从训练到推理），`README`要写明训练脚本和推理脚本。
- 读入文件的路径尽量使用相对路径，比如`./data/XX`。
- 请在代码中包含适当的注释以方便代码审核。
- 应尽可能保证代码的简洁，删除与最终算法和模型无关的调试代码。
- **若不符合上述规范要求或实验结果无法复现，由此导致的后果由选手承担**。

代码文件夹结构示例：

```Plaintext
project
    |--code
        |-- main.py or main.py or run.sh or ...
        |-- README.md
        |-- ...
    |--checkpoint
        |-- model_1
        |-- model_2

```

## 结果输出文件夹 /result

```Plaintext
project
    |-- result  
        |-- prediction.json

```

`prediction.json` 必须为 UTF-8 编码的合法 JSON 文件，顶层结构为字典：

```JSON
{
  "ZHXWDXXX6XX-0XXXXX-GXXXXXUYINGXXX": [
    {
      "bbox": [843, 2087, 93, 89],
      "text": "天"
    },
    {
      "bbox": [808, 2147, 97, 96],
      "text": "王"
    }
  ],
  "ZHXXXXXXXX-XXXXXX-GUJIXXXXUYINGXXXX": []
}

```

## 最终提交

所有代码和数据最终被打包为一个zip 文件