# 镜像提交平台页面分析（结构化文档）

> 为自动化 Agent / 脚本生成的结构化说明文档，清晰描述页面交互、规则与字段定义。

------

## 1. 模态框：镜像信息填写表单

该表单为配置镜像的交互弹窗，包含 3 个必填 / 选填字段与 2 个操作按钮。

表格







| 字段 / 控件 | 类型       | 描述与规则                                                   |
| ----------- | ---------- | ------------------------------------------------------------ |
| `镜像路径`  | 文本输入框 | **必填**。Docker 镜像的完整地址（例如阿里云镜像服务地址 `registry.cn-hangzhou.aliyuncs.com/namespace/repo:tag`）。公开镜像地址可直接填写，私有镜像需配合用户名 / 密码使用。 |
| `用户名`    | 文本输入框 | **选填**。私有镜像仓库的登录用户名（公开镜像无需填写）。     |
| `密码`      | 密码输入框 | **选填**。私有镜像仓库的登录密码（公开镜像无需填写）。       |
| `取消`      | 按钮       | 关闭弹窗，放弃本次配置，返回主页面。                         |
| `确定`      | 按钮       | 提交表单数据，验证并保存镜像配置。                           |

### 表单提示文案

> 推荐使用阿里云私有镜像服务，公开镜像地址可不必填写用户名及密码。

------

## 2. 主页面核心规则（提示区）

### 2.1 提交与执行规则

- **提交次数限制**：队伍当日剩余提交次数为 `3次`，提交结果实时返回。

- 脚本与文件规则

  ：

  1. 执行脚本 `run.sh` 必须放置在容器内的 `/app` 目录。
  2. 预测结果文件必须输出到容器内的 `/saisresult` 目录，且统一命名为 `prediction.json`。
  3. 输入数据集路径：脚本需从容器内的 `/saisdata` 目录读取数据。

- **截止时间**：镜像提交截止时间：`2026-05-27 14:00:00`（逾期无法提交）。

### 2.2 页面控件

表格







| 控件                 | 功能                                                         |
| -------------------- | ------------------------------------------------------------ |
| `镜像路径: 暂无数据` | 显示当前已配置的镜像地址，未配置时显示默认文本。             |
| `配置路径`           | 点击触发上方的镜像信息填写弹窗，用于新增 / 修改镜像配置。    |
| `提交`               | 触发镜像构建与任务执行，提交后消耗一次当日提交次数。         |
| `运行日志`           | 可折叠面板，点击可展开查看提交任务的执行日志，支持刷新重试。 |

------

## 3. 完整交互流程（Agent 可直接执行）

1. 点击主页面的「配置路径」按钮，弹出镜像填写弹窗。
2. 填写 `镜像路径`（必填），若为私有镜像，同步填写`用户名`和`密码`。
3. 点击「确定」保存配置，主页面的`镜像路径`字段会更新为填写的地址。
4. 确认配置无误后，点击「提交」按钮，触发任务执行（消耗 1 次当日提交次数）。
5. 提交后可展开「运行日志」面板，查看执行过程与结果。
6. 任务执行完成后，系统会校验 `/saisresult/prediction.json` 文件是否生成，并返回实时结果。

------

## 4. 关键约束与注意事项

- 容器内路径为固定规则，

  不可修改

  ：

  - 脚本路径：`/app/run.sh`
  - 输出路径：`/saisresult/prediction.json`
  - 输入路径：`/saisdata/`

- 公开镜像无需填写用户名密码，私有镜像必须填写，否则拉取镜像会失败。

- 提交次数每日重置，当日最多可提交 3 次，请谨慎操作。

- 必须在 `2026-05-27 14:00:00` 前完成提交，逾期系统关闭提交入口。

------

## 5. 错误排查点（Agent 可按此定位问题）

1. 镜像拉取失败：检查镜像路径格式是否正确，私有镜像的用户名 / 密码是否填写正确。
2. 脚本执行失败：确认 `run.sh` 放置在 `/app` 目录，且有可执行权限（`chmod +x run.sh`）。
3. 结果文件未生成：确认脚本执行成功，且输出文件路径为 `/saisresult/prediction.json`。
4. 提交次数不足：检查当日剩余提交次数，需次日重置或联系管理员。