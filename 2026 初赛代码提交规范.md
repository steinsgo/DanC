# 2026 初赛代码提交规范

初赛采用 **Docker 镜像提交**。比赛平台启动选手镜像后，会自动挂载评测图片目录，选手程序需要自动完成推理，并将结果保存到 `/saisresult/prediction.json`。最终评分以项目中的 `evaluate.py` 为准。

## 数据文件夹 `/saisdata`

选手无需提交初赛评测数据。比赛平台会将评测图片挂载到镜像内：

```text
/saisdata/13/eval/images/
├── image_id_1.png
├── image_id_2.png
└── ...

```

说明：

- 评测图片格式为 PNG。
- 评测图片数量约 1000 张。
- 图片文件名去掉 `.png` 后缀后，即为提交结果中的 `image_id`。
- 评测数据不包含标注文件。
- `/saisdata` 为平台输入目录，选手程序不应向该目录写入结果文件。

## 代码与模型文件

- 选手需要将推理所需的代码、模型权重、配置文件、字典文件等全部打包进 Docker 镜像。
- 镜像启动后应自动执行推理流程，不需要人工交互。
- 推理程序应从 `/saisdata/13/eval/images/` 读取图片，并将结果写入 `/saisresult/prediction.json`。
- 不建议在正式评测时依赖公网下载模型、数据或代码。
- 所使用的依赖环境应在 `README.md` 中写明，包括操作系统、Python 版本、CUDA/cuDNN 版本、PyTorch/PaddlePaddle/PaddleOCR 等关键库版本。
- 如使用 GPU，应在 `README.md` 中注明相关 CUDA 和深度学习框架版本。
- 请在 `README.md` 中说明模型方案、模型权重位置、推理入口、训练方式和外部数据使用情况。

## 结果输出文件夹 `/saisresult`

选手程序必须生成如下文件：

```text
/saisresult/
└── prediction.json

```

`prediction.json` 必须为 UTF-8 编码的合法 JSON 文件，顶层结构为字典：

```json
{
  "ZHXWDXXX6XX-0XXXXX-GXXXXXUYINGXXX": [
    {
      "bbox": [843, 2087, 93, 89],
      "text": "天"
    },
    {
      "bbox": [808, 2147, 97, 96],
      "text": "王"
    }
  ],
  "ZHXXXXXXXX-XXXXXX-GUJIXXXXUYINGXXXX": []
}

```

字段说明：

| 字段       | 说明                              |
| ---------- | --------------------------------- |
| `image_id` | 图片文件名去掉 `.png` 后缀后的 ID |
| `bbox`     | 字符检测框，格式为 `[x, y, w, h]` |
| `text`     | 识别出的字符文本                  |

`bbox` 中，`x`、`y` 为检测框左上角坐标，`w`、`h` 为宽和高，单位为像素。请勿使用 `[x1, y1, x2, y2]` 格式。

如果某张图片没有检测到字符，建议仍保留该图片 ID，并将结果写为空列表。

## 评分规则

最终评分脚本为 `evaluate.py`。评分时会将选手预测结果与标准答案逐图比较：

- 预测框与真实框的 IoU 大于等于 0.5，才可能匹配成功。
- 匹配成功后，预测 `text` 与真实 `text` 完全一致，记为 TP。
- 误检、重复预测、检测位置错误或文字识别错误，计入 FP。
- 漏检或未被正确识别的真实字符，计入 FN。

最终分数为整体 F1：

```text
Precision = TP / (TP + FP)
Recall    = TP / (TP + FN)
F1        = 2 * Precision * Recall / (Precision + Recall)

```

## 代码的随机性

对于用到随机数的步骤，请固定随机种子，保证同一镜像在同一输入数据上多次运行结果稳定。

如果因未固定随机性导致多次运行结果波动较大，赛事方可根据多轮运行结果进行复核。

## 附注

提交前请确认：

- 镜像可以正常启动并自动运行推理。
- 输入目录为 `/saisdata/13/eval/images/`。
- 输出文件为 `/saisresult/prediction.json`。
- `prediction.json` 可以被 `json.load` 正常读取。
- `prediction.json` 顶层为字典，每个图片 ID 对应一个列表。
- 每个预测项包含 `bbox` 和 `text` 字段。
- `bbox` 为 `[x, y, w, h]`，`text` 为字符串。
- 镜像内包含推理所需的全部代码、模型和配置文件。